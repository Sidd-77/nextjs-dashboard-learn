import {Inter, Pixelify_Sans, Lusitana} from "next/font/google"

export const inter = Inter({ subsets: ['latin']});

export const pixel = Pixelify_Sans({ subsets: ['latin']});

export const lusitana = Lusitana({ weight: ['700', '400'], subsets: ['latin']});
